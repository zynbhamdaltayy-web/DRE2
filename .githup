- name: Build project
        run: npm run build

      - name: Check build output
        run: |
          echo "=== DIST FILES ==="
          find ./dist -maxdepth 2 -type f

      - name: Setup Pages
        uses: actions/configure-pages@v5